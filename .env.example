# ================================================
# 환경 변수 템플릿 (.env.example)
# ================================================
# 이 파일은 로컬 개발 환경용 템플릿입니다
# 프로덕션 환경의 .env는 GitHub Actions에서 자동으로 생성됩니다
#
# 로컬 개발 환경 사용법:
#   1. 이 파일을 .env로 복사: cp .env.example .env
#   2. .env 파일을 열어서 실제 카카오 API 키 등을 입력
#   3. 애플리케이션 실행 시 환경 변수로 자동 로드됨
#
# 주의:
#   - .env 파일은 절대 Git에 커밋하지 마세요!
#   - 이 .env.example 파일은 Git에 커밋해도 안전합니다 (예시 값만 포함)
# ================================================

# ========== JWT 설정 ==========
# JWT 비밀 키 (최소 64자 이상, HS512 알고리즘용)
# 프로덕션 환경에서는 강력한 무작위 문자열을 사용하세요
JWT_SECRET=BMcSH2UO4wpASQaYYAH3bL0f7WEFmr576qbm4K9MQ8Ljj0bhUIImvY4tiVfK0VkUuFIjSihM7WVCN5IEjnVtrA==

# Access Token 만료 시간 (밀리초)
# 기본값: 3600000 = 1시간
JWT_ACCESS_TOKEN_EXPIRATION=3600000

# Refresh Token 만료 시간 (밀리초)
# 기본값: 604800000 = 7일
JWT_REFRESH_TOKEN_EXPIRATION=604800000


# ========== 카카오 OAuth 설정 ==========
# 카카오 REST API 키 (카카오 개발자 콘솔에서 발급)
# https://developers.kakao.com/console/app
KAKAO_CLIENT_ID=your_kakao_client_id_here

# 카카오 Client Secret (카카오 개발자 콘솔에서 발급)
KAKAO_CLIENT_SECRET=your_kakao_client_secret_here

# 카카오가 인증 후 호출하는 백엔드 콜백 URI (카카오 개발자 콘솔에 등록해야 함)
# ⚠️ 반드시 /api 접두사를 포함해야 합니다 (컨트롤러 매핑: /api/auth/kakao/callback)
# 개발: http://localhost:9080/api/auth/kakao/callback
# 프로덕션: http://server-ip-address/api/auth/kakao/callback (nginx 80포트 경유 권장)
KAKAO_REDIRECT_URI=http://localhost:9080/api/auth/kakao/callback

# 프론트엔드 URL (CORS 및 OAuth 리다이렉트용)
# 개발: http://localhost
# 프로덕션: http://server-ip-address
FRONTEND_URL=http://localhost


# ========== 데이터베이스 설정 (로컬 개발 환경용) ==========
# 주의: 아래는 로컬 개발 환경용 예시 값입니다
# 프로덕션 환경의 DB 설정은 GitHub Secrets에서 관리됩니다

# 로컬 MySQL 데이터베이스 연결 설정
DB_URL=jdbc:mysql://localhost:3306/mannal?useSSL=false&serverTimezone=Asia/Seoul&connectionTimeZone=Asia/Seoul&forceConnectionTimeZoneToSession=true&allowPublicKeyRetrieval=true
DB_USERNAME=root
DB_PASSWORD=1234


# ================================================
# 요약
# ================================================
#
# 로컬 개발 환경:
#   - 위의 예시 값을 참고하여 .env 파일을 만들어 사용
#   - 카카오 API 키는 실제 발급받은 값으로 변경 필요
#   - DB는 로컬 MySQL 사용 (localhost:3306)
#
# 프로덕션 환경 (AWS Lightsail):
#   - GitHub Actions가 GitHub Secrets에서 값을 가져와 자동으로 .env 생성
#   - deploy.yml 파일 참고
#   - GitHub Secrets에 다음 값들이 설정되어 있어야 함:
#     * DB_USERNAME, DB_PASSWORD
#     * JWT_SECRET
#     * KAKAO_CLIENT_ID, KAKAO_CLIENT_SECRET, KAKAO_REDIRECT_URI
#     * FRONTEND_URL
#
# 주의사항:
#   - .env 파일은 절대 Git에 커밋하지 마세요!
#   - 이 .env.example 파일은 Git에 커밋해도 안전합니다 (예시 값만 포함)
# ================================================
